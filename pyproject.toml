[project]
name = "taiwan-travel-agent"
version = "0.1.0"
description = "LangGraph + FastAPI + Claude API 台灣旅遊 AI Agent"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn>=0.23.0",
    "anthropic>=0.20.0",
    "langchain>=0.1.0",
    "langgraph>=0.1.0",
    "chromadb>=0.5.0",
    "sqlalchemy>=2.0.0",
    "pydantic-settings>=2.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "httpx>=0.24.0",
]
